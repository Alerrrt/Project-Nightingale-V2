{
  "name": "cors_misconfiguration_scanner",
  "signals": {
    "headers": ["access-control-allow-origin", "access-control-allow-credentials", "access-control-expose-headers"]
  },
  "mapping": {
    "cwe": {
      "CWE-942": { "keywords": ["access-control-allow-origin", "credentials", "cors"] }
    },
    "owasp": {
      "A01": { "keywords": ["broken access control", "credentials", "origin *"] },
      "A05": { "keywords": ["security misconfiguration"] }
    }
  },
  "remediation": "Do not use wildcard origins with credentials. Return a specific origin, validate against an allowlist, and omit credentials unless strictly required."
}


